# Required by AGP for any module depending on AndroidX (Compose, activity-compose, ...).
android.useAndroidX=true

# Keep R classes non-transitive (AGP 8 default; stated explicitly so it can't drift).
android.nonTransitiveRClass=true

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
